anthropic>=1.6.0
python-dotenv>=1.0.0
pytest>=8.0.0

# Calendar agent — M365
msal>=1.28.0
requests>=2.32.0

# Calendar agent — Google
google-auth>=2.29.0
google-api-python-client>=2.126.0

# News agent — news-agent skill (YAML config; feedparser only for live RSS fetch)
pyyaml>=6.0
feedparser>=6.0
